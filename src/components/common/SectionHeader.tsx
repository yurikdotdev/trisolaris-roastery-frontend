const SectionHeader = ({ title }: { title: string }) => {
  return <h1 className="font-inter text-3xl font-bold mt-8 p-8 text-custom-textLight dark:text-custom-textDark">{title}</h1>;
};

export default SectionHeader;
